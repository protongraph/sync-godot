[plugin]
name="ProtonGraph Sync"
description="Synchronize the results from the standalone ProtonGraph software directly in the editor. Requires the standalone app to run at the same time."
author="HungryProton"
version="0.0.1"
script="plugin.gd"
